<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
    <Properties>
        <Property name="logPattern">%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n</Property>
        <Property name="logDir">logs</Property>
    </Properties>

    <Appenders>
        <!-- Console Appender -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="%highlight{${logPattern}}{FATAL=red, ERROR=red, WARN=yellow, INFO=green, DEBUG=cyan}"/>
        </Console>

        <!-- File Appender - todos os logs -->
        <RollingFile name="File" fileName="${logDir}/bilhetagem.log"
                     filePattern="${logDir}/bilhetagem-%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout pattern="${logPattern}"/>
            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
                <SizeBasedTriggeringPolicy size="10MB"/>
            </Policies>
            <DefaultRolloverStrategy max="30"/>
        </RollingFile>

        <!-- File Appender - apenas erros -->
        <RollingFile name="ErrorFile" fileName="${logDir}/errors.log"
                     filePattern="${logDir}/errors-%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout pattern="${logPattern}"/>
            <Filters>
                <ThresholdFilter level="ERROR" onMatch="ACCEPT" onMismatch="DENY"/>
            </Filters>
            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
                <SizeBasedTriggeringPolicy size="5MB"/>
            </Policies>
            <DefaultRolloverStrategy max="30"/>
        </RollingFile>
    </Appenders>

    <Loggers>
        <Root level="DEBUG">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="File"/>
            <AppenderRef ref="ErrorFile"/>
        </Root>
        
        <!-- Reduzir verbosidade de bibliotecas externas -->
        <Logger name="org.apache.poi" level="WARN"/>
        <Logger name="org.jfree" level="WARN"/>
        <Logger name="org.sqlite" level="WARN"/>
    </Loggers>
</Configuration>